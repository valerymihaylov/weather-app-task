# Weather App using React + Vite

**Setup**

  1. Clone repository:
    ```git clone https://github.com/valerymihaylov/weather-app-task.git```
  2. Install dependencies:
     ```npm install```
  4. Run development server:
     ```npm run dev```
